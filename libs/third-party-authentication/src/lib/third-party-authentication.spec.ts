import { thirdPartyAuthentication } from './third-party-authentication';

describe('thirdPartyAuthentication', () => {
  it('should work', () => {
    expect(thirdPartyAuthentication()).toEqual('third-party-authentication');
  });
});
